name: "CodeQL"

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  schedule:
    - cron: '0 6 * * 1'

permissions:
  actions: read
  contents: read
  security-events: write

jobs:
  analyze:
    name: Analyze
    runs-on: ubuntu-latest
    timeout-minutes: 360
    steps:
      - uses: actions/checkout@v7

      - name: Set up JDK 21
        uses: actions/setup-java@v6
        with:
          distribution: temurin
          java-version: 21

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v6.3.0

      # Pinned to a specific CodeQL bundle: the runner default currently caps at Kotlin < 2.4.20,
      # while bundle v2.27.1 ships the Java/Kotlin library 9.3.1 that adds Kotlin 2.4.20 support.
      - name: Initialize CodeQL
        uses: github/codeql-action/init@codeql-bundle-v2.27.1
        with:
          languages: java-kotlin

      - name: Build
        run: ./gradlew assembleDebug test

      - name: Perform CodeQL Analysis
        uses: github/codeql-action/analyze@codeql-bundle-v2.27.1